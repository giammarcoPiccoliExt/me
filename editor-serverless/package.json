{
  "name": "serverless-docs-editor",
  "version": "1.0.0",
  "description": "A serverless, frontend-only documentation editor using Vite and TypeScript.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
